# AI Model Files

Place all trained or pre-trained model files here.

## Expected contents

- [ ] Model files: `.tflite`, `.onnx`, `.pt`, etc.
- [ ] One model card per model in `../docs/Model_Cards.pdf`
- [ ] If total size > 100 MB: provide `download_models.sh` script (HuggingFace / GitHub Releases)

## Directory layout (suggested)

```
models/
├── pose_estimation/
├── object_detection/
└── download_models.sh   # if needed
```

## Model inventory

| File | Test(s) | Size (MB) | Format |
|------|---------|-----------|--------|
| _[To be added]_ | | | |
